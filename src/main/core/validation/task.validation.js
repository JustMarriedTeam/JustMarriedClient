export const taskDescription = value => !/^.*$/im.test(value) ? 'Type valid task description' : undefined; //eslint-disable-line
export const taskName = value => !/^.*$/im.test(value) ? 'Type valid task description' : undefined; //eslint-disable-line
