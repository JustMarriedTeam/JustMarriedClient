import React, { PureComponent } from 'react';
import Layout from '../../layout/Layout';

export default class WeddingPage extends PureComponent {

  render() {
    return (
      <Layout>
        configuration
      </Layout>
    );
  }

}
